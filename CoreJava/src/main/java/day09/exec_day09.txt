1，定义一个电梯类型，包含一个start()方法，代表电梯的运行，在start()方法中产生一个0-100的随机整数值i，如果i%3==0,则在start()方法中抛出一个超载异常，如果i%5==0,则在start()方法中抛出一个钢缆断裂异常；要求分别定义这两个异常类。其中超载异常是未检查异常，而钢缆断裂异常是已检查异常。

2，定义一个大楼类，该类有一个电梯对象作为属性，大楼有一个方法run()，在run()方法中调用电梯对象的start()方法，要求处理已检查异常。


3、把前面的石头剪刀布游戏改写为用匿名内部类实现。

