1、TicketType，代表机票类型，有三个枚举值：成人票，儿童票，婴儿票。定义一个属性，double priceOff，代表不同机票类型的折扣。该属性值通过构造方法传入成人票1.0，儿童票0.3，婴儿票0.1。

2、CabinClass，代表仓位等级，包含三个枚举值，头等舱，公务舱，经济舱。有一个属性，double priceOff，代表不同舱位等级的价格系数。该属性值通过构造方法传入。
FirstClass=1.8;
OfficialClass=1.3;
EconomyClass=1.0;

3、定义一个枚举，在达内学的课程。
每门课程有一个老师姓名属性，并且有一个构造方法和获得老师名的方法
为每一个枚举值实现一个study方法，为每门课定义不同的学习方式。