server:
  port: 8040
spring:
  application:
    name: microservice-gateway-zuul
eureka:
  client:
    service-url:
      defaultZone: http://node1:8761/eureka/
  instance:
    prefer-ip-address: true
zuul:
  ribbon:
    eager-load:
      enabled: true